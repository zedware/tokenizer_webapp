import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Tokenizer Webapp - Compare AI Model Tokenizers",
  description: "Compare tokenization between different AI models including OpenAI, HuggingFace, Google Gemini, and traditional NLP tokenizers",
  keywords: ["tokenizer", "openai", "gpt", "gemini", "huggingface", "bert", "nlp", "ai", "language model"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}